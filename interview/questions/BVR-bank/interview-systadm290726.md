System-Administration 270 руки 
1. есть два датацентра, в одном и другом две вм, соединены через впн. На одно поднимается бд в контейнере мастер, в другом бд слейв. Контейнер через контейнер поднимается. Репликация падает. С точки зрения системы всё в порядке. Всё пингуется.
Ответ: разный mtu у контейнера и впн (у впна меньше). Нужно уменьшить mtu контейнера.
2. Может ли под из одного нс обратиться к поду из другого нс?
Ответ: можно через внутренний днс
3. Как ограничить это?
Ответ: через политики и crd
4. что такое gitops?
Ответ: определение состояние кластера через git-репозитории.
5. что такое gitflow?
Ответ: разделение разработки по веткам git.
6. вопросы про опыт работы с виртуализацией (vmware, proxmox, zvirt), elc, k8s, kafka, graylog, jenkins. Сколько нод, получилось ли самому потрогать руками.
Ответ:
7. Самая большая проблема с которой столкнулись
Ответ:
8. Сравнить puppet и ansible.
Ответ: puppet сравнивает фактическую конфигурацию с той, что на сервере (идеальной), из коробки. Ansible просто прогоняет конфигурацию, ansible реализует фичи puppet через cron.
9. Иерархия переменных ansible.